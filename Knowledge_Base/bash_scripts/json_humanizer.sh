#The JSON Log Parser

 #   The Goal: Take a JSON log file (like from your Chat App) and extract only the message and timestamp fields into a clean, readable text file.

  #  Key Commands: jq (This is the most important tool for DevOps—Google it!).

   # Why: Most cloud logs are JSON; you need to know how to slice them.

   