services:
  redis:
    image: 'redis:alpine'
    ports:
      - 6379:6379

  web:
    build: .
    ports:
      - "5000:5000"
    depends_on:
      - redis
    environment:
     - REDIS_HOST=redis

    volumes:
      - redis_data:/data  
volumes:
  redis_data: